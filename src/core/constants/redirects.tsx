export const redirectToHome = {
  redirect: {
    destination: '/',
    permanent: false,
  },
};

export const redirectToLogin = {
  redirect: {
    destination: '/auth',
    permanent: false,
  },
};

export const redirectToUnauthorized = {
  redirect: {
    destination: '/',
    permanent: false,
  },
};
